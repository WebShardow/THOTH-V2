# Database Configuration
DB_USER=myuser
DB_PASSWORD=mypassword_change_me
DB_NAME=mydatabase

# Next Auth Configuration
NEXTAUTH_SECRET=generate_a_random_string_here
NEXTAUTH_URL=http://localhost:3000

# Other Configuration
# DATABASE_URL is constructed in docker-compose: postgresql://${DB_USER}:${DB_PASSWORD}@db:5432/${DB_NAME}
